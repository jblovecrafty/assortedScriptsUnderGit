#!/usr/bin/python
import BitVector

bitVectorSize 			= 10
numberOfHashFunctions 	= 3

#set up bit array size m
#
bitVector = BitVector(size = bitVectorSize)

#set up hashing functions number of them is k
#


#take input and hash it k times
#take the result of each hash and set the nth bit in the bit array

#code to query the database

#code to print out accuracy of bloom filter at current time
#do this by looping thru the bit array and taking all the ones (saturation) and squaring that by 